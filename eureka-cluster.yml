server:
  port: 8761
spring:
  cloud:
    config:
      allowOverride: true
      failFast: true
      overrideNone: false
      overrideSystemProperties: false
  profiles: default
  application:
    name: eureka-server-first.com
  boot:
    admin:
      context-path: /admin
management:
  endpoints:
    web:
      exposure:
        include: "*"
eureka:
  server:
    eviction-interval-timer-in-ms: 3000 # remove immediately service list
    response-cache-update-interval-ms: 1000 # Eureka Server's Response Cache. Default 30,000ms
    enableSelfPreservation: false           # Just for demo
  instance:
    hostname: eureka-server-first.com
    appname: eureka-cluster
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://eureka-server-second.com:8762/eureka/,http://eureka-server-third.com:8763/eureka/,http://eureka-server-fourth.com:8764/eureka/
logging:
  level:
    com:
      netflix:
        eureka: OFF
        discovery: OFF      
---
server:
  port: 8762
spring:
  profiles: second
  boot:
    admin:
      context-path: /admin
eureka:
  server:
    eviction-interval-timer-in-ms: 3000 # remove immediately service list
    response-cache-update-interval-ms: 1000 # Eureka Server's Response Cache. Default 30,000ms
    enableSelfPreservation: false           # Just for demo
  instance:
    hostname: eureka-server-second.com
    appname: eureka-cluster
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://eureka-server-first.com:8761/eureka/,http://eureka-server-third.com:8763/eureka/,http://eureka-server-fourth.com:8764/eureka/
logging:
  level:
    com:
      netflix:
        eureka: OFF
        discovery: OFF  
management:
  endpoints:
    web:
      exposure:
        include: "*"  
---
server:
  port: 8763
spring:
  profiles: third
  boot:
    admin:
      context-path: /admin
eureka:
  server:
    eviction-interval-timer-in-ms: 3000 # remove immediately service list
    response-cache-update-interval-ms: 1000 # Eureka Server's Response Cache. Default 30,000ms
    enableSelfPreservation: false           # Just for demo
  instance:
    hostname: eureka-server-third.com
    appname: eureka-cluster
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://eureka-server-first.com:8761/eureka/,http://eureka-server-second.com:8762/eureka/,http://eureka-server-fourth.com:8764/eureka/
logging:
  level:
    com:
      netflix:
        eureka: OFF
        discovery: OFF
management:
  endpoints:
    web:
      exposure:
        include: "*"
---
server:
  port: 8764
spring:
  profiles: fourth
  boot:
    admin:
      context-path: /admin
management:
  endpoints:
    web:
      exposure:
        include: "*"
eureka:
  server:
    eviction-interval-timer-in-ms: 3000 # remove immediately service list
    response-cache-update-interval-ms: 1000 # Eureka Server's Response Cache. Default 30,000ms
    enableSelfPreservation: false           # Just for demo
  instance:
    hostname: eureka-server-fourth.com
    appname: eureka-cluster
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://eureka-server-third.com:8763/eureka/,http://eureka-server-second.com:8762/eureka/,http://eureka-server-first.com:8761/eureka/
logging:
  level:
    com:
      netflix:
        eureka: OFF
        discovery: OFF 